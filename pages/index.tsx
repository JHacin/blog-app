import Head from 'next/head';

const Home = (): JSX.Element => (
  <div>
    <Head>
      <title>Blog App</title>
      <link rel="icon" href="/favicon.ico" />
    </Head>

    <div>Hello</div>
  </div>
);

export default Home;
